# Server runtime

NODE_ENV=development
PORT=3000

# Frontend origin used for CORS once API/WebSocket features are added.
WEB_ORIGIN=http://localhost:5173

# Database is planned for a later milestone.
DATABASE_URL=postgresql://sketchroom:sketchroom@localhost:5432/sketchroom?schema=public
