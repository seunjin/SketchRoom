# Server runtime

NODE_ENV=development
PORT=3000

# Frontend origin used for CORS once API/WebSocket features are added.
WEB_ORIGIN=http://localhost:5173

# Local PostgreSQL runs through Docker Compose on host port 15432.
DATABASE_URL=postgresql://sketchroom:sketchroom@localhost:15432/sketchroom?schema=public
