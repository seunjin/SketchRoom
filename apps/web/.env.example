# Vite exposes only variables prefixed with VITE_ to browser code.

VITE_APP_NAME=SketchRoom

# Local development uses the Vite proxy configured in vite.config.ts.
# Production should set this to the deployed server URL.
VITE_API_URL=/api
VITE_WS_URL=ws://localhost:3000/ws
