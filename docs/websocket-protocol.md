# WebSocket 프로토콜

이 문서는 SketchRoom의 WebSocket 메시지 프로토콜을 정리하기 위한 문서다.

초기 워크스페이스 단계에서는 아직 WebSocket 기능을 구현하지 않는다.

## 추후 정리할 내용

- 연결/해제 흐름
- 클라이언트에서 서버로 보내는 이벤트
- 서버에서 클라이언트로 보내는 이벤트
- 1:1 채팅 라우팅 방식
- 채팅방 브로드캐스트 방식
- Canvas stroke 동기화 방식
- 에러와 acknowledgement 메시지
- reconnect와 heartbeat 처리 방식
